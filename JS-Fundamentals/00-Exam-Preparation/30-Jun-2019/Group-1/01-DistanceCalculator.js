function distanceCalculator(array) {
    const stepsMade = Number(array.shift());
    const lengthOfOneStep = Number(array.shift());
    const distanceNeeded = Number(array.shift());
}
distanceCalculator(['100', '2', '2']);